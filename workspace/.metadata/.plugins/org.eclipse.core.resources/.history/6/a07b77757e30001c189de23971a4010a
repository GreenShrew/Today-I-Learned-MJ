package days02;
/*
  클래스 이름은 영문과 숫자가 조합되어서 만들어질수도 있다.
 다만, 반드시 첫글자가 영문이어야 한다.
 이름 중간에 공백 또는 특수기호가 들어갈 수 없다.
 사용 가능한 특수 기호가 있다면 언더바(_) 정도만 사용한다.
*/
public class JavaSecond {

	public static void main(String[] args) {
		System.out.printf("겁나 쉬운 자바 프로그래밍!\n");
		System.out.printf("1\t겁나 쉬운 자바 프로그래밍!\n");
		System.out.printf("12\t겁나 쉬운 자바 프로그래밍!\n");
		/* 
		문자열 내에서 특수한 기능을 수행하는 문자.
		큰따옴표 안에 내용을 쓰면 내용이 출력되지만, 예외로 명령을 내려 조작할 수 있게 만든다.
		escape 문자를 쓸 때는 \ 를 쓴다.
		\n : new line. 새 줄을 
		\t : Tab을 누른 것처럼 일정간격 띄움. 항상 이동하는 위치가 정해져있다.
		\' : 작은 따옴표 출력
		\" : 큰 따옴표 출력
		%% : % 출력
		\\ : 백슬래쉬( \ ) 출력
		\b : 백스페이스 	- 커서 왼쪽으로 이동하면서 앞글자를 한 글자씩 삭제.
		 					- 다만, 이클립스 콘솔창에서는 정상작동하지 않는다.
		 */
		
		System.out.printf("\n");		// 한줄 다음으로..
		// \t를 활용하는 방법.
		System.out.printf("--------------------------------------------------\n");
		System.out.printf("번호\t국어\t영어\t수학\t총점\t평균\n");
		System.out.printf("--------------------------------------------------\n");
		System.out.printf("1\t89\t87\t56\t254\t81.5\n");
		System.out.printf("2\t53\t77\t100\t111\t60.5\n");
		System.out.printf("3\t29\t23\t300\t400\t70\n");
		System.out.printf("--------------------------------------------------\n");

		System.out.printf("\n");		// 한줄 다음으로..
		// 문자열 내에 ""를 출력하는 방법.
		System.out.printf("겁나 쉬운 \"자바\" 프로그래밍!\n");
		// 큰따옴표를 화면에 직접 출력하려면 \"fkrh Tjdi zmsEkdhavyaks cnffurehlsek.
		// \ 없이 "만 사용한다면 끝나는 따옴표로 인식되어 오류를 발생시킨다.
		System.out.printf("겁나 쉬운 \'자바\' 프로그래밍!\n");
		System.out.printf("겁나 쉬운 '자바' 프로그래밍!\n");
	}
	// 입력, 연산, 출력 중 출력을 해 보았다.
}

/*
 영문 1글자는 1byte
 한글, 한자 2byte
*/