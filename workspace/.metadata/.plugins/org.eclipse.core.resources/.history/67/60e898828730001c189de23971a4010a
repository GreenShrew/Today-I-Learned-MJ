package days02;

public class Printf01 {

	public static void main(String[] args) {
		// 출력하고자 하는 내용을 "" 안에 자유롭게 구성하여 출력할 수 있다.
		// 숫자와 문자가 조합된 출력물을 화면에 출력한다.
		// 키보드가 허락하는 한 어떤 내용이든 구성하여 출력할 수 있다.
		System.out.printf("56 x 69 = 1234\n");		// 내용이 거짓이어도 출력이 된다. 즉, 이 상태로는 계산을 해주지는 않는다.
		
		// 이스케이프 문자 - % : 아직 출력의 내용으로 결정되지 않은 데이터의 출력자리를 마련해둔다.
		// %d : 정수, %f : 실수, %s : 문자열 등
		// 그리고 그 자리에 출력될 내용은 ""가 끝난 후 뒤에 컴마로 구분하고 써준다.
		System.out.printf("56 x 69 = %d\n", 1234);
		
		// 간단한 연산을 컴퓨터에게 시키고 그 결과를 포함한 내용을 출력할 수 있다.
		System.out.printf("56 x 69 = %d\n", 56*69);
		// 오해하지 말자!
		// 56 x 69 = 56*69 X				56 x 69 = 3864 O
		
		// % 이스케이프 문자는 갯수에 상관없이 필요한 만큼 구성하여 사용이 가능하다.
		System.out.printf("%d x %d = %d\n", 23, 87, 23*87);
		// " " 안의 % 갯수와 순서대로 출력될 데이터를 나열하면 순서대로 매칭되어 출력된다.
		
		// 소수점이 있는 실수의 출력
		System.out.printf("%d ÷ %d = %d\n", 27, 13, 27/13);		// 참고로, 13.0이 아니라 13으로 쓰면 정수가 결과로 나온다. 이 부분은 나중에 더 배울 것이다.
		// ÷ : 출력되기 위한 연산기호. 실제 연산과는 무관하다.
		// / : 실제 연산을 위한 연산기호.
		// 덧셈 : +, 뺄셈 : -, 곱셈 : *, 나눗셈 : /  을 사용한다.
	}

}
// printf를 이용할 때는 어떻게 이용해야하는가.
/*
 1. 단순한 연산을 시키고 그 결과를 포함한 내용을 출력할 수 있다.
 */